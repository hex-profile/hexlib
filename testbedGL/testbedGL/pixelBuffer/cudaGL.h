#pragma once

#if defined(__GNUC__)
    #pragma GCC diagnostic push
    #pragma GCC diagnostic ignored "-Wpedantic"
#endif

#include <cudaGL.h>

#if defined(__GNUC__)
    #pragma GCC diagnostic pop
#endif
