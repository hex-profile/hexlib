#================================================================
#
# Common app implementations:
# * Depends on C++ runtime.
# * Depends on STL.
# * Not shell-specific, valid both for GUI and console shells.
#
#================================================================

hexlibProjectTemplate(hexlibShellCommon hexlib::shellCommon "hexlibBaseLib;hexlibImgBase;hexlibModuleTools")
